# 複製此檔為 .env 後依需求修改（不設定也能用，程式內有預設值）

# 伺服器
PORT=3000
NODE_ENV=development

# JWT 設定
JWT_SECRET=change-this-in-production
JWT_EXPIRES_IN=1d

# 存放 JWT 的 cookie 名稱
COOKIE_NAME=token

# 預設帳號（啟動時自動建立，可在此覆寫）
SEED_USER_NAME=Demo User
SEED_USER_EMAIL=demo@example.com
SEED_USER_PASSWORD=demo1234
