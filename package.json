{
  "name": "todo-api-openapi-postman",
  "version": "1.0.0",
  "private": true,
  "description": "教學用：Node.js + Express + EJS + Tailwind 的 Todo List RESTful API（JWT 認證、記憶體資料）",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "concurrently -n server,css -c blue,magenta \"npm:dev:server\" \"npm:dev:css\"",
    "dev:server": "nodemon src/server.js",
    "dev:css": "tailwindcss -i ./src/styles/input.css -o ./public/css/output.css --watch",
    "build:css": "tailwindcss -i ./src/styles/input.css -o ./public/css/output.css --minify",
    "docs:build": "node scripts/generate-openapi.js",
    "postman:build": "node scripts/generate-postman.js",
    "postman:test": "newman run postman/collection.json -e postman/environment.json",
    "postman:report": "newman run postman/collection.json -e postman/environment.json -r htmlextra --reporter-htmlextra-export report.html"
  },
  "keywords": [
    "todo",
    "restful",
    "express",
    "ejs",
    "jwt",
    "tailwind"
  ],
  "license": "MIT",
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^8.5.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "swagger-parser": "^10.0.3",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "concurrently": "^10.0.3",
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1",
    "nodemon": "^3.1.14",
    "openapi-to-postmanv2": "^6.3.0",
    "tailwindcss": "^4.3.0"
  }
}
